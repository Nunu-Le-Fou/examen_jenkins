apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "k-api-orders.fullname" . }}-test-connection"
  labels:
    {{- include "k-api-orders.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "k-api-orders.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
